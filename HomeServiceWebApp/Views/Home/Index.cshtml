@{
    ViewBag.Title = "Home Page";
}


<div class="container" style="margin-top: 20px;">
    <div class="row">
        <div class="col-lg-6">
            @Html.ActionLink("Register User", "UserRegister", "Account")
        </div>
        <div class="col-lg-6">
            @Html.ActionLink("Register User", "UserRegister", "Account")
        </div>
        <div class="col-lg-6">
            @Html.ActionLink("Register User", "UserRegister", "Account")
        </div>
    </div>
</div>
